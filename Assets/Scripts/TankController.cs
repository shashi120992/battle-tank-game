using Assets.Scripts;
using UnityEngine;

public class TankController
{
    public TankModel tankModel { get; }
    public TankView tankView { get; }
   
    /*
    public TankController(TankModel_model,TankView_viewPrefab)
    {
        tankModel = _model;
        tankView = GameObject.Instantiate<TankView>(_viewPrefab);

    }
    public void Move(float Time)
    {
        tankView.transform.position
    }
   */
}
